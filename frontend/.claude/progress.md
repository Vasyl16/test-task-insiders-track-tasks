# Progress

## Completed Tasks
- Scaffolded via `npm create vite@latest` (React + TypeScript), then stripped of the default template's demo content down to a minimal shell (`f16cb8e`).
- Tailwind CSS v4 added via `@tailwindcss/vite` (`2f52821`).
- Frontend documentation structure created (`CLAUDE.md` + `.claude/`), mirroring the backend's documentation-driven workflow, independent of `backend/CLAUDE.md`.
- **V1 Auth UI — Step 1: Axios instance, interceptors, token manager.**
  - Installed `axios` (only — the query/cache library and React Router are deferred to the steps that actually use them, per `CLAUDE.md`'s "confirm before installing" note; their naming in the target tree strongly implies TanStack Query and React Router, but that wasn't explicitly confirmed, so nothing beyond Axios was added yet).
  - `api/axios/token-manager.ts`: get/set/clear access + refresh tokens, backed by `localStorage`, as the single place token storage happens.
  - `api/axios/instance.ts`: one configured Axios instance, base URL from `VITE_API_URL` (new `.env`/`.env.example`, defaults to `http://localhost:3000/api`).
  - `api/axios/interceptors.ts`: request interceptor attaches `Authorization: Bearer <accessToken>`; response interceptor catches `401`, uses a bare (non-instance) `axios.post` to call `/auth/refresh` so the refresh call itself can't recursively trigger the interceptor, deduplicates concurrent refreshes behind a single in-flight promise (important given the backend rotates refresh tokens — two simultaneous refreshes would otherwise invalidate each other), then retries the original request once with the new token. Clears tokens and gives up if there's no refresh token or the refresh itself fails.
  - Added `.env`/`.env.example` (`VITE_API_URL`) and gitignored `.env`, matching the backend's env-file pattern.
  - Verified end-to-end in a real headless browser (Playwright, installed ephemerally via `npx`/`npm install --no-save` — not added to `package.json`) against the live backend: unauthenticated `GET /auth/me` → 401; `POST /auth/register` → 201; authenticated `GET /auth/me` → 200 with the correct email; a deliberately corrupted access token still resulted in 200 with the correct email (proving the interceptor's catch-401-refresh-retry cycle actually works, not just compiles); confirmed the refresh token was rotated. The temporary verification code that drove this (a `useEffect` in `App.tsx` calling the API and rendering the results) was removed afterward — `App.tsx` is back to the plain shell; this kind of ad-hoc API call doesn't belong in a page component per `CLAUDE.md`'s API conventions.
- **V1 Auth UI — Step 2: Application foundation.**
  - Libraries confirmed and installed: `react-router` (v8 — the unified package; `react-router-dom` is now the legacy/frozen name, so imports are from `react-router` directly), `@tanstack/react-query`, `react-hook-form`, `zod`, `@hookform/resolvers` (the standard RHF↔Zod glue, added now since it's needed the moment real forms exist).
  - `api/queryKeys.ts`: minimal cache-key factory, currently just `auth.me`.
  - `providers/QueryProvider.tsx`: `QueryClient` with `retry: 1`, `refetchOnWindowFocus: false`, `staleTime: 60_000`.
  - `context/AuthContext.ts` + `hooks/useAuth.ts` + `providers/AuthProvider.tsx`: split into three files (not one) because ESLint's `react-refresh/only-export-components` rejects a component file that also exports a hook — this is the correct fix, not a suppression, and matches `architecture.md`'s own separation of `context/`, `hooks/`, and `providers/`. `AuthProvider` hydrates `user` via `GET /auth/me` (through TanStack Query) only if an access token already exists in `localStorage` at mount; exposes `user`, `isAuthenticated`, `isLoading`, `logout`. `logout` calls `POST /auth/logout` with the stored refresh token, then clears tokens and evicts the cached query.
  - Known limitation, deliberately deferred: `hasToken` is only read once at mount, not reactive. Once login/register are implemented (next step), they'll need to invalidate `queryKeys.auth.me` after storing new tokens for `isAuthenticated` to flip without a full page reload.
  - `routes/ProtectedRoute.tsx` / `PublicRoute.tsx`: redirect to `/login` / `/dashboard` respectively based on `useAuth()`, rendering nothing while the initial `/auth/me` check is in flight.
  - `routes/router.tsx`: `/login` + `/register` under `PublicRoute` → `AuthLayout`; `/dashboard` under `ProtectedRoute` → `DashboardLayout`; `/` redirects to `/dashboard` (which itself redirects to `/login` when unauthenticated).
  - `layouts/AuthLayout.tsx` (centered card) and `layouts/DashboardLayout.tsx` (header with email + logout button, `<Outlet />` body) — minimal Tailwind styling, no real design system yet.
  - `pages/auth/{LoginPage,RegisterPage}.tsx` and `pages/dashboard/DashboardPage.tsx`: placeholder headings only, no forms — as instructed.
  - `App.tsx` now composes `QueryProvider > AuthProvider > RouterProvider`; this is also how the Axios instance/interceptors get pulled into the startup path (imported transitively via `AuthProvider`), satisfying "integrate the Axios client into application startup" without needing a separate bootstrap step.
  - Verified: `tsc -b` and `npm run build` both clean, and a real headless-browser check (no backend needed, since these are unauthenticated placeholder-page checks) confirmed all four navigation cases: `/` → `/login`, `/register` → renders "Register", `/dashboard` → `/login` (guard works), `/login` → renders "Login". No page errors.
- **V1 Auth UI — Step 2 follow-up: replace Context/Provider with Zustand, move the query into `api/services`.**
  - Removed `context/AuthContext.ts` and `providers/AuthProvider.tsx` entirely — Zustand doesn't need a wrapping provider component, so `App.tsx` is back to just `QueryProvider > RouterProvider`.
  - `api/services/useGetUser.ts`: the `/auth/me` `useQuery` call, moved out of the (now-deleted) provider and into the services layer per `architecture.md` — this is the pattern going forward for query-backed hooks.
  - `store/authStore.ts`: a small Zustand store holding just the `logout` action (revokes the refresh token, clears local tokens, evicts the cached `auth.me` query). Deliberately *not* storing `user`/`isAuthenticated` in Zustand — that's server state and stays owned by TanStack Query's cache (already shared across every component that calls `useGetUser`/`useAuth`, so there was nothing Context was doing that the query cache wasn't already doing).
  - `hooks/useAuth.ts` now composes `useGetUser()` + `useAuthStore()` into the same `{ user, isAuthenticated, isLoading, logout }` shape as before — `ProtectedRoute`, `PublicRoute`, and `DashboardLayout` needed no changes.
  - Pulled the `QueryClient` instance out of `providers/QueryProvider.tsx` into `api/queryClient.ts` (same `react-refresh/only-export-components` reason as the earlier Context/hook split) so `authStore.ts` can import it directly to evict queries on logout.
  - Hit a real bug during this: `npm install zustand` reported success but the package never actually landed in `package.json`/`node_modules` — a later unrelated `npm install --no-save playwright` silently pruned it as "extraneous" since the lockfile was never updated. Caught it via the same browser verification (Vite failed to resolve `"react"` imported by `zustand`, i.e. the package wasn't there at all), reinstalled it properly, and confirmed it stuck in `package.json` this time before re-verifying.
  - Verified again after the fix: `tsc -b`, lint, `npm run build`, and the same four-route headless-browser check all pass with no errors.
  - Not yet committed — held back intentionally so it can be verified/reviewed before committing, per instruction.
- **V1 Auth UI — Step 2 correction: auth is server state, not Zustand state.**
  - Reversed the Zustand-holds-`user` design from the previous entry after discussion. Settled principle: TanStack Query owns *all* server state, including the authenticated user — a single `["auth","me"]` query is the one source of truth. Zustand is reserved strictly for client/UI-only state (theme, sidebar, modals, etc.) and currently has **no store at all**, since no such UI state exists yet. `zustand` stays installed (it's confirmed as part of the intended stack for that future need), but `store/authStore.ts` was deleted.
  - `api/queries/authQueries.ts`: plain `getMe()` — no React, no hook, just the HTTP call.
  - `api/mutations/authMutations.ts`: plain `logoutRequest()` — calls `POST /auth/logout` with the stored refresh token, swallows its own network errors (logout must succeed client-side regardless of whether the server call does).
  - `api/services/useAuth.ts`: the real hook. `useQuery(['auth','me'], getMe, { enabled: Boolean(tokenManager.getAccessToken()), retry: false })` plus a `useMutation(logoutRequest)` whose `onSuccess` clears tokens and updates the cache. Exposes `user`, `isAuthenticated: Boolean(user)`, `isLoading`, `isError`, `logout`.
  - Deleted `hooks/useAuth.ts`, `store/authStore.ts`, and the `useEffect`/`hydrate()` bootstrap in `App.tsx` — none of that manual wiring is needed anymore; `useQuery` fetches automatically the first time `useAuth()` renders anywhere.
  - `queryKeys.ts` has `auth.me` again.
  - **Real bug found and fixed during verification, not just a design choice:** the logout `onSuccess` originally called `queryClient.removeQueries({ queryKey: queryKeys.auth.me })`. This actually cleared the cache correctly, but — confirmed via console instrumentation in a real browser — did **not** trigger a re-render of already-mounted `useQuery` observers (`ProtectedRoute` stayed on `/dashboard` after logout, tokens were cleared, but nothing reacted). Switching to `queryClient.setQueryData(queryKeys.auth.me, null)` fixed it immediately and reliably — a direct cache write does notify observers, where `removeQueries` apparently doesn't (at least not reliably, on this `@tanstack/react-query` 5.101.2 / React 19.2 combination). This is now the pattern to reuse for the login/register flow too: after storing tokens, call `queryClient.setQueryData(queryKeys.auth.me, user)` directly (register's response already includes the user; login will need one `getMe()` call first) rather than `invalidateQueries`/`removeQueries`.
  - Verified end-to-end in a real browser against the live backend, with two separate browser contexts (learned from an earlier test mistake — `addInitScript` re-seeds on every navigation in the same context, which had made an earlier logout check look broken when it wasn't): registering + visiting `/dashboard` with a valid token renders correctly with the right email; clicking "Log out" clears tokens **and** correctly redirects to `/login` in the same session (no reload); a fresh context with no token at all correctly redirects `/dashboard` → `/login`. Re-ran the original four-route navigation check too — still clean. No console/page errors in any case.
  - Follow-up rename: `api/queries/auth.queries.ts` → `authQueries.ts` and `api/mutations/auth.mutations.ts` → `authMutations.ts` (dot-case → camelCase), to match the naming already used for `useAuth.ts`, `queryClient.ts`, `queryKeys.ts`. Rebuilt/linted clean after.
- **Architecture migration: flat `api/`-first layout → pragmatic Feature-Sliced Design.**
  - Restructured `src/` into `app / pages / widgets / features / entities / shared`, per updated project instructions, while keeping the centralized API layer (now under `shared/api`) instead of splitting HTTP code per feature.
  - Moved (via `git mv` for already-tracked files, so history follows the rename): `api/axios/*` → `shared/api/axios/*`; `api/queryKeys.ts` → `shared/api/queryKeys.ts`; `layouts/*` → `app/layouts/*`; `providers/QueryProvider.tsx` → `app/providers/QueryProvider.tsx`; `routes/*` → `app/routes/*`; `App.tsx` → `app/App.tsx`; `types/user.ts` → `entities/user/model/user.ts`.
  - Merged `api/queries/authQueries.ts` + `api/mutations/authMutations.ts` (both still uncommitted from the previous entry) into a single `shared/api/queries/auth.ts` — per the new spec, `queries/` holds plain HTTP functions with no query/mutation split, so there's no separate `mutations/` folder anymore. `shared/api/services/useAuth.ts` and `shared/api/queryClient.ts` moved as-is.
  - `widgets/`, `features/`, and most of `shared/` beyond `api/` were deliberately **not** created — there's no business logic yet to put in them (login/register are still placeholder pages), and empty scaffolded folders were explicitly out of scope.
  - Judgment calls made and confirmed with the user before executing: `entities/user/` created now (rather than deferring to `shared/types/`) since `User` is a real, growing domain entity; `AuthLayout`/`DashboardLayout` placed under `app/layouts/` (not a new `widgets/`) since they're app-wide shells wired into the router, not composite business widgets.
  - All internal imports updated to match (six files: `useAuth.ts`, `DashboardLayout.tsx`, `ProtectedRoute.tsx`, `PublicRoute.tsx`, `router.tsx`, `QueryProvider.tsx`, `main.tsx`). No behavior changes.
  - Verified: `tsc -b` clean, `npm run lint` clean, `npm run build` clean, and the same four-route headless-browser check as the previous entry (`/` → `/login`, `/register` → renders "Register", `/dashboard` → `/login`, `/login` → renders "Login") — all pass, no console/page errors.
  - `.claude/architecture.md`, `.claude/conventions.md`, and `CLAUDE.md` updated to describe the new layer structure.
- **V1 Auth UI — Step 3: real login/register forms, plus first `features/` and `widgets/` folders.**
  - `shared/api/queries/auth.ts`: added `login(credentials)` (`POST /auth/login` → `{accessToken, refreshToken}`) and `register(credentials)` (`POST /auth/register` → `{user, accessToken, refreshToken}`), matching the backend's actual `AuthTokensDto`/`RegisterResponseDto` contracts (checked directly against `backend/src/modules/auth/`, not assumed).
  - `shared/api/services/useAuth.ts`: added `useLogin()` and `useRegister()` mutations. Both store tokens via `tokenManager.setTokens` then write the resulting user straight into the `auth.me` cache with `queryClient.setQueryData` (same confirmed-working pattern as logout, not `invalidateQueries`) — login has no user in its response, so it does one extra `getMe()` call after storing tokens; register's response already includes the user.
  - `shared/lib/getErrorMessage.ts`: new — first file in `shared/lib`. Small helper that pulls the backend's actual error message (`error.response.data.message`) out of an Axios error, falling back to a generic string. Generic enough to not be auth-specific, hence `shared/lib` and not `features/auth`.
  - `features/auth/` created (first real feature folder): `schemas/loginSchema.ts` + `schemas/registerSchema.ts` (Zod; register adds a client-only `confirmPassword` with a `.refine` check — the backend has no such field), `components/LoginForm.tsx` + `components/RegisterForm.tsx` (React Hook Form + `zodResolver`, wired to `useLogin`/`useRegister`, error surfaced via `getErrorMessage`).
  - **Real bug caught during verification, not just a design choice:** the backend's `ValidationPipe` runs with `forbidNonWhitelisted: true` (`backend/src/main.ts`), so posting the register form's raw values (including the client-only `confirmPassword`) to `/auth/register` returned `400 Bad Request`. Fixed by having `RegisterForm`'s submit handler strip `confirmPassword` before calling `useRegister()` — only `{email, password}` ever leaves the form.
  - `pages/auth/LoginPage.tsx` / `RegisterPage.tsx` now just render `<LoginForm />` / `<RegisterForm />` — pages still don't touch HTTP directly.
  - `widgets/app-header/` created (first widget): `ui/AppHeader.tsx` is the dashboard header (app name + current user email + logout button) extracted out of `app/layouts/DashboardLayout.tsx`, since it composes an entity (the user's email) with a feature (auth logout) — a textbook widget rather than a plain layout shell. `DashboardLayout` now just renders `<AppHeader />` + `<Outlet />`.
  - Verified end-to-end against the live backend (headless browser, not just typecheck): register with a fresh email → tokens stored → cache updated → **auto-redirects to `/dashboard`** (no explicit `navigate()` call needed — `PublicRoute`'s already-mounted `useAuth()` observer reacts to the `setQueryData` write and redirects on its own, the same mechanism proven for logout, now confirmed symmetric for login/register); dashboard shows the correct email; logout → `/login`; logging back in with the same credentials → `/dashboard` again with the correct email; wrong password → stays on `/login` and surfaces the backend's real `"Invalid credentials"` message (not a generic fallback); empty/invalid register submission is caught client-side by Zod (no network request sent) with the right per-field messages, including the password-mismatch case. No console/page errors in any case except the deliberate 401 from the wrong-password test.
  - Verified: `tsc -b`, `npm run lint`, `npm run build` all clean throughout.
  - Not yet committed — held back pending review, per instruction.
- **V1 Auth UI — Step 4: first `shared/ui` primitives, forms/header refactored to use them.**
  - `shared/ui/Button.tsx`: `variant: 'primary' | 'ghost'` (defaults to `'primary'`); `type` defaults to `'button'` but is overridable (forms pass `type="submit"`).
  - `shared/ui/Input.tsx`: labeled input + inline error message in one component; `forwardRef` so it composes directly with React Hook Form's `{...register('field')}` (which needs the `ref`).
  - `shared/ui/FormError.tsx`: renders a form-level (non-field) error line, or nothing if there's no message.
  - `LoginForm.tsx`/`RegisterForm.tsx` refactored to use `Input`/`Button`/`FormError` instead of hand-repeated Tailwind classes for every field (the three fields previously duplicated the same label/input/error markup verbatim). `widgets/app-header/ui/AppHeader.tsx`'s logout button now uses `<Button variant="ghost">`.
  - No behavior change — re-verified the same register → dashboard → logout flow end-to-end against the live backend (headless browser + screenshots of the login and dashboard screens to confirm the labeled inputs and button variants actually render correctly, not just typecheck).
  - Verified: `tsc -b`, `npm run lint`, `npm run build` all clean.
  - Not yet committed — held back pending review, per instruction.

- **V1 Auth UI — Step 5: `Button` refinements (hover states, a dedicated `logout` variant, disabled cursor).** Small, incremental changes never separately documented before now — bundled into this entry since they were never committed on their own either.
  - Added `transition-colors` + `hover:bg-gray-800` to `primary`, `hover:text-gray-900` clarified on `ghost`.
  - New `logout` variant: a red pill (`bg-red-50`/`text-red-600`, `hover:bg-red-100`) — `widgets/app-header`'s logout button switched from `ghost` to this.
  - `disabled:cursor-not-allowed` added as a base class shared by all variants (Tailwind doesn't force `cursor: pointer` on buttons by default on this version, but nothing was overriding the browser default for the *disabled* case either — a loading/disabled button looked identical to an active one). Also `disabled:hover:bg-gray-900` on `primary` so the hover-darken doesn't fire while disabled.
  - Verified via computed-style checks in a real browser (`getComputedStyle(button).cursor` before/after setting `disabled`) and visual screenshots of both variants' hover states.

## V2/V3 Workspace + Project UI
- **First real feature work beyond auth.** Builds the frontend for the backend's Workspace CRUD + membership and Project CRUD (both already complete API-side) so the whole vertical slice is visible in the browser. Member invitation UI was explicitly deferred (matches the backend's own "invite messages later" framing) — this pass is create/list/view/delete only.
- `entities/workspace/model/workspace.ts`, `entities/project/model/project.ts`: plain types mirroring the backend's response DTOs (`id`, `name`, `description: string | null`, `ownerId`/`createdBy`, timestamps).
- `shared/api/queries/{workspace,project}.ts`: `get*`/`create*`/`update*`/`delete*` per domain, matching the backend routes exactly (`project.ts`'s functions all take `workspaceId` first, since every Project endpoint is nested under `/workspaces/:workspaceId/projects`). Both files' `create`/`update` share a small `toRequestBody` helper that turns an empty/whitespace-only `description` into `undefined` rather than sending `""` — keeps the optional field genuinely optional (Prisma stores `null`) instead of coercing it to an empty string.
- `shared/api/queryKeys.ts`: added `workspaces.all` / `workspaces.detail(id)` and `projects.all(workspaceId)` / `projects.detail(workspaceId, id)`, following the existing key-factory pattern.
- `shared/api/services/{useWorkspaces,useProjects}.ts`: `useQuery` for list/detail, `useMutation` for create/update/delete, standard `invalidateQueries` on success (not the `setQueryData` pattern used for auth — that was specifically needed because `removeQueries` doesn't reliably notify observers; plain list invalidation doesn't have that problem).
  - **Real bug found and fixed during verification, not just a design choice:** deleting a workspace while its own detail page (`WorkspacePage`) was still mounted caused two spurious `404`s (`GET /workspaces/:id`, `GET /workspaces/:id/projects`), confirmed via response-monitoring in a real browser. Root cause: TanStack Query's `invalidateQueries` fuzzy-matches by key *prefix* by default, so invalidating `['workspaces']` also matched and refetched the still-mounted detail/projects queries for the workspace that had just been deleted server-side. Fixed two ways together: (1) `useDeleteWorkspace`/`useDeleteProject` now pass `exact: true` on their list invalidation and explicitly `removeQueries` the specific now-gone detail key; (2) `WorkspacePage`'s delete handler now calls `navigate('/dashboard')` **before** firing the mutation (fire-and-forget `mutate`, not awaited `mutateAsync`) so the page unmounts — and its queries stop being "active" — before the mutation's `onSuccess` cache operations run. Either fix alone wasn't sufficient (an active/mounted query observer refetches even after `removeQueries` clears its cache entry); both together closed it. Re-verified after the fix: zero failed requests, zero console errors across the full create → view → create-project → delete-project → delete-workspace flow.
- `features/workspace/{schemas/workspaceSchema.ts,components/CreateWorkspaceForm.tsx}` and `features/project/{schemas/projectSchema.ts,components/CreateProjectForm.tsx}`: same RHF + Zod + `Input`/`Button`/`FormError` pattern as `LoginForm`/`RegisterForm`, one file each since there's no update-form yet (deferred — see below).
- `shared/ui/Button.tsx`: removed the `w-full` baked into `primary` (was fine when its only consumers were full-width auth forms; wrong for this UI's inline "New workspace"/"New project" buttons). `LoginForm`/`RegisterForm` now pass `className="w-full"` explicitly to keep their existing look — no visual change there, just the default un-hardcoded.
- `pages/dashboard/DashboardPage.tsx`: no longer a placeholder — lists the current user's workspaces (`useWorkspaces()`), each linking to `/workspaces/:id`, plus a toggleable `CreateWorkspaceForm`.
- `pages/workspace/WorkspacePage.tsx` (new): workspace name/description, a `Delete workspace` button gated on `workspace.ownerId === user.id` (client-side UI gating only — the backend is the real enforcement), its list of projects (`useProjects`), a toggleable `CreateProjectForm`, and a per-project `Delete` button gated on `isOwner || project.createdBy === user.id` (mirrors the backend's own creator-or-owner rule).
- New route: `/workspaces/:workspaceId` under the same `ProtectedRoute`/`DashboardLayout` as `/dashboard`.
- **Deliberately not built this pass:** editing a workspace's/project's name or description (`Update*Dto` support exists API-side, just no UI yet), and anything member-invitation-related. Both are natural, low-effort follow-ups reusing the same form components.
- Verified end-to-end against the live backend in a real headless browser: register → empty-state dashboard → create workspace → appears in list → open it → shows name/description/delete-button → create project → appears → delete project → empty-projects state → delete workspace (with confirm) → back to empty-state dashboard. Also checked persistence across logout/login (two workspaces created, both still there after a fresh login) and confirmed zero failed network requests / zero console errors across the whole run, not just typecheck.
- Verified: `tsc -b`, `npm run lint`, `npm run build` all clean.
- Committed as `261158d`.
- **Visual redesign — "The Ledger Desk" design system**, via the `frontend-design` skill. Subject: workspaces are ledgers, projects are entries logged inside them. Fixed a real UX bug in the same pass: the workspace detail page had no way back to the dashboard.
  - Palette (named): `desk` `#161c26` (dark ink-navy page canvas), `paper` `#eae3d2` (card/modal surfaces), `ink` `#1b2129` (text on paper), `brass` `#b8863b` + `brass-light`/`brass-deep` (the signature accent), `oxblood` `#8c3b32` (destructive actions), `fog` `#8791a3` (muted text on the dark canvas). Type: **Fraunces** (display, used narrowly for titles/wordmark), **IBM Plex Sans** (body), **IBM Plex Mono** (labels, timestamps, entry numbers) — loaded via Google Fonts in `index.html`. All defined as Tailwind v4 `@theme` tokens in `index.css`, so `bg-desk`/`text-brass`/`font-display` etc. are ordinary utility classes everywhere.
  - Signature element: a small brass corner tab/notch, repeated on workspace cards, the auth card, and every modal — the one recognizable shape tying the system together (per the skill's "spend your boldness in one place" principle).
  - `shared/ui/Modal.tsx` (new): a real portal-rendered dialog (`createPortal` to `document.body`) — backdrop click and `Escape` both close it, body scroll is locked while open, entrance is a quick scale/fade (`motion-safe:` prefixed, so `prefers-reduced-motion` gets no animation at all, not a reduced one). `CreateWorkspaceForm`/`CreateProjectForm` now render inside a `Modal` instead of an inline toggle block on the page — this is what "make it a separate beautiful modal" asked for.
  - `pages/workspace/WorkspacePage.tsx`: added the missing back link (`← All workspaces`, a plain `react-router` `Link`) — the actual bug fix. Redesigned the project list as ledger rows: a running entry number (`01`, `02`, …), ruled hairline dividers, a quiet `Remove` text-action instead of a loud delete button. The entry numbers are **genuinely chronological** (oldest = `01`) even though the API returns newest-first — reversed at the display layer (`[...projects].reverse()`) specifically so the numbering isn't decorative-but-wrong.
  - `widgets/app-header/ui/AppHeader.tsx`: redesigned — wordmark "Ledger" in Fraunces italic (links home to `/dashboard`), a small-caps "Workspaces" eyebrow, user email in mono, logout as a quiet nav-style action.
  - `shared/ui/Button.tsx`: added a 4th variant, `nav` — quiet text action correctly readable on the **dark** desk background, distinct from `ghost` (which is tuned for on-**paper** contexts like modal Cancel buttons). Needed because `ghost`'s `text-ink/60` would have been unreadable directly on `bg-desk`.
  - **Two real bugs caught via screenshot self-review, not just typecheck** (the skill explicitly calls for taking screenshots to self-critique): (1) the `primary` button variant was `bg-ink` — on the paper cards/modals that's a normal dark button, but the dashboard's "New workspace"/"New project" buttons sit directly on the `bg-desk` canvas, where dark-on-dark made the button nearly invisible. Fixed by switching `primary` to a `bg-brass` fill, which reads clearly against both paper and desk. (2) the ledger entry-number bug described above (found by deliberately creating two projects in a known order and checking the screenshot matched).
  - Verified end-to-end in a real browser: full register → create workspace (modal) → open it → back link works → create/delete project (numbers correct, both directions) → delete workspace flow, plus a modal-Escape-to-close check. Zero failed requests, zero console errors. `tsc -b`/`lint`/`build` all clean.
  - Also, separately, made the modal's close button bigger (`h-9 w-9` circular hit area with a hover background) after it was flagged as too small.
  - Committed as `fcf51d9`.
- **V4 Task Management UI.** Extends the ledger design system to a new domain: a project is now a page of its own (`pages/project/ProjectPage.tsx`, route `/workspaces/:workspaceId/projects/:projectId`) with its tasks as numbered ledger rows, same pattern as Workspace → Project.
  - `entities/task/model/task.ts`: `Task` type plus `taskStatusValues` (the canonical `['TODO','IN_PROGRESS','DONE']` tuple) and `TASK_STATUS_LABELS` — both defined once here and imported by `features/task/schemas/taskSchema.ts` and the UI, rather than each redeclaring its own copy.
  - `entities/workspace/model/workspace-member.ts`: `WorkspaceMember` type (`id`, `workspaceId`, `role`, `createdAt`, nested `user: {id, email}`) — first frontend consumer of the backend's member-listing endpoint, needed to populate the task assignee dropdown.
  - `shared/api/queries/workspace.ts`: added `getWorkspaceMembers`. `shared/api/queryKeys.ts`: added `workspaces.members(id)` and `tasks.all/detail(...)`.
  - `shared/api/queries/task.ts` + `shared/api/services/useTasks.ts`: full CRUD, same shape as Project's. One deliberate signature difference: `useUpdateTask(workspaceId, projectId)` takes `id` **per mutate-call** (`updateTask.mutate({ id, status })`), not at hook-creation time — matching `useDeleteTask`'s existing shape — specifically so a single hook instance in `ProjectPage` can update any row in the task list without calling a hook once per row (which the Rules of Hooks don't allow inside `.map()`).
  - `shared/ui/Select.tsx` (new): a labeled `<select>` styled to match `Input` exactly (same label treatment, border, focus ring) — needed for the task status and assignee fields, which `Input` (text-only) can't represent.
  - `features/task/{schemas/taskSchema.ts,components/CreateTaskForm.tsx}`: same RHF + Zod pattern as the other Create*Forms, plus two `Select`s (status, defaulting to `TODO`; assignee, populated from `useWorkspaceMembers`, defaulting to "Unassigned").
  - `pages/project/ProjectPage.tsx` (new): back link to the workspace (mirrors `WorkspacePage`'s back link to the dashboard), delete-project button (creator-or-owner), and tasks as ledger rows — entry number (genuinely chronological, same reversal technique as Project's list), title (struck through + dimmed when `DONE`), description, resolved assignee email (or "Unassigned"), an inline status `<select>` (any workspace member can use this — matches the backend's collaborative-update rule), and a `Remove` action gated the same as Project's (creator-or-owner).
  - `pages/workspace/WorkspacePage.tsx`: each project row's title/description is now a `Link` to its `ProjectPage` (previously inert list-item text — there was nowhere to go from a project until this page existed).
  - Verified end-to-end in a real browser: register → workspace → project → task creation with description+status+assignee → inline status change (confirmed the `line-through` class actually lands on `DONE` by reading the rendered DOM, not just the screenshot) → assignee email resolves and displays → removing one of two tasks removes the *correct* one (confirmed by title, not just count) → zero failed requests, zero console errors throughout. Two apparent failures in an earlier test run turned out to be flaws in the test script itself (a bad Playwright selector, and a stale locator reference), not real bugs — re-verified directly against the DOM to be sure before concluding that.
  - Verified: `tsc -b`, `npm run lint`, `npm run build` all clean.
  - Test-data cleanup used the strict `zztest-` prefix convention only; confirmed the 4 pre-existing real workspaces under the developer's own account were untouched afterward.
  - Not yet committed — held back pending review.

- **V4 follow-up — Kanban board + task priority.** Requested explicitly: drag task cards between To do / In progress / Done, plus a priority indicator (green/yellow/red).
  - `entities/task/model/task.ts`: added `taskPriorityValues`, `TaskPriority`, `TASK_PRIORITY_LABELS`, and `TASK_PRIORITY_DOT_CLASSES` (a lookup from priority → Tailwind background class, since dynamic class-name string interpolation doesn't work with Tailwind's build-time class extraction — the map has to use literal class names Tailwind can see statically).
  - `index.css`: one new token, `--color-moss` (a muted sage green) for `LOW` priority. `MEDIUM` reuses `brass`, `HIGH` reuses `oxblood` — deliberately not adding three brand-new colors for what's fundamentally a traffic-light *signal*, not a brand accent. This is the one place the system intentionally borrows literal semantic color (green/amber/red) rather than staying inside the ledger palette, since a priority indicator needs to be instantly scannable in a way a themed color wouldn't be.
  - `widgets/task-board/ui/TaskBoard.tsx` (new): the Kanban board itself, replacing the flat ledger-row task list on `ProjectPage`. Three columns (one per `TaskStatus`, in order), each a `bg-desk-raised` container (first real use of that token, defined early on but unused until now) holding `bg-paper` task cards — dark column, warm card, matching the "index cards on a lamp-lit desk" concept the palette was built around.
  - **Drag-and-drop is native HTML5 DnD (`draggable`, `onDragStart`/`onDragOver`/`onDrop`), not a library.** Deliberate: this codebase has stayed dependency-light throughout (confirming libraries before installing, per `CLAUDE.md`), and native DnD covers the ask without a new package. The dragged task's id travels via `event.dataTransfer`, not React state — state (`draggedTaskId`) is used only for the visual opacity-while-dragging effect, since relying on state alone for the actual drop payload would be fragile (native drag events are the browser's own mechanism for this, and reading through `dataTransfer` is the standard/correct way to do it, not just an implementation preference).
  - **Kept an accessible fallback**: each card still has the same inline status `<select>` from the previous (flat-list) design, so status changes don't require a mouse or fine motor control — drag is an enhancement, not the only way to move a card. This was a deliberate quality-floor decision, not an oversight (native drag-and-drop alone would exclude keyboard/screen-reader users).
  - Priority renders as a small colored dot (`title="Priority: ..."` for a hover tooltip) rather than a colored badge/pill — the skill's "status as typography, not new colors" principle extended: a dot is a quiet signal, not a decoration competing with the brass tab as the page's signature element.
  - Dropped the numbered-entry-line device for tasks specifically (kept for the flat Project list in `WorkspacePage`) — once tasks live in three columns instead of one chronological list, a running number no longer encodes a real sequence per the skill's own numbering rule, so it was removed rather than kept as decoration.
  - `CreateTaskForm`: added a priority `Select`, defaulting to `MEDIUM`.
  - Verified end-to-end in a real browser: created three tasks with LOW/MEDIUM/HIGH priority, confirmed the three dot colors render correctly; dragged a card from To do to In progress via actual mouse-down/move/up simulation (not just calling the update function) and confirmed both the column counts and the card's own status-select updated; changed a task to Done via the accessible select and confirmed strikethrough + dimmed styling. Zero failed requests, zero console errors throughout. Test-data cleanup used the `zztest-kanban` prefix only.
  - Verified: `tsc -b`, `npm run lint`, `npm run build` all clean.
  - Committed alongside the backend priority commit's frontend counterpart.
- **V4 follow-up — custom priority picker, top-border priority indicator, task editing.** Three explicit requests in one pass.
  - `shared/ui/Listbox.tsx` (new): a fully custom dropdown, not a styled native `<select>`. Trigger button shows the selected option's colored dot + label + a chevron that rotates on open; the open panel is a `<ul role="listbox">` of button-per-option rows (colored dot, brass-tinted highlight on the current selection), closes on outside click, `Escape`, or selecting an option. Deliberately generic (`options: {value, label, dotClassName}[]`) rather than priority-specific, so it carries no domain knowledge — matches `shared/ui`'s existing rule — and `features/task` is what configures it with `TaskPriority` values/colors. Wired into React Hook Form via `Controller` (not `register()`, since it isn't a native form element) — first use of `Controller` in this codebase.
  - Accessibility for the custom listbox is baseline, not full ARIA-combobox: native `<button>` semantics per option mean Tab+Enter/Space work without extra code, plus `Escape`-to-close and `aria-expanded`/`aria-haspopup`/`role="listbox"`/`role="option"`/`aria-selected`. No roving-tabindex arrow-key navigation — judged not worth the extra complexity for a 3-option list; would reconsider for a longer list.
  - `entities/task/model/task.ts`: added `TASK_PRIORITY_BORDER_CLASSES` (`border-t-moss`/`border-t-brass`/`border-t-oxblood`) alongside the existing dot-class map.
  - `widgets/task-board/ui/TaskBoard.tsx`: replaced the small priority dot with a `border-t-4` on the card itself, colored per priority. Since a color-only signal fails accessibility (WCAG: don't convey information by color alone), added a plain-text priority label (`"High · assignee@example.com"`) on the card too — the border is the "beautiful at a glance" version, the text is the same information for anyone who can't perceive the color difference.
  - `features/task/components/EditTaskForm.tsx` (new): same field set and validation as `CreateTaskForm` (title, description, status, priority via the new `Listbox`, assignee), pre-filled from the existing task, submitting through `useUpdateTask`. Kept as a separate component from `CreateTaskForm` rather than a shared one parameterized by mode — the two are similar but not identical (different defaults, different submit copy, "Save changes" vs "Log task"), and this codebase's `Create*Form`s have all been standalone so far.
  - `TaskBoard`: added an `Edit` action next to `Remove` on every card, opening `EditTaskForm` in a `Modal`. Unlike `Remove` (creator-or-owner only), `Edit` is visible to **every** workspace member — matching the backend's own permission model (any member can update a task's fields, only creator/owner can delete it). This wasn't a new rule invented for editing; it's the same rule the inline status `<select>` already relied on, just now covering the full field set instead of only status.
  - Verified end-to-end in a real browser: opened the custom listbox and confirmed it shows all three colored options with the current one highlighted; created a task with High priority via the listbox (not the old native select) and confirmed the card's top border rendered red; opened Edit, changed the title and dropped priority to Low via the listbox again, saved, and confirmed the card updated to the new title with a green top border and updated text label. Zero failed requests, zero console errors. `tsc -b`/`lint`/`build` all clean.
- **Reverted, same session: removed the `Edit` action from the Kanban board.** Explicit follow-up feedback — the board should stay drag-only for status, not grow a second modal-based editing path. Deleted `features/task/components/EditTaskForm.tsx` entirely (it had no other caller once the button was gone, so it would've been dead code) and removed the `editingTask` state / `Modal` wiring from `TaskBoard`. The inline status `<select>` and `Remove` action stay — those predate this feature and serve a different purpose (accessible status change, and deletion), not general editing. `shared/ui/Listbox.tsx` stays too, since `CreateTaskForm` still uses it for the priority picker; only the edit-specific consumer went away. Re-verified: `Edit` button count is 0, `Remove`/status-`<select>` still present and working, `tsc -b`/`lint`/`build` clean.
- **Follow-up correction, same session: removed the status `<select>` from each card too.** A momentary miscommunication — an in-progress `git revert` of the `Edit`-removal commit was interrupted and replaced with `git reset --hard` back to that commit (the revert would have restored `EditTaskForm`, which is not what was wanted). The actual ask: status should change **only** by dragging a card between columns, nothing else. Removed the accessible status `<select>` and its `sr-only` label from `TaskBoard`'s card; the only remaining per-card control is `Remove` (creator-or-owner), right-aligned on its own. This does trade away the keyboard/non-drag-user accessibility path for status changes — a deliberate choice made explicitly by the user, not an oversight; worth revisiting only if raised again.
  - A note on process, not the feature: the first verification attempt of this change made it look like drag-and-drop itself had broken (the card didn't move). It hadn't — Playwright's synthetic mouse-based drag simulation is sensitive to move speed/step count against real HTML5 drag events, and a slower, more incremental mouse path (more steps, small pauses) reproduced the successful move. Confirmed via screenshot before concluding either way, rather than assuming the first result was authoritative.
  - Verified: `Edit` button count 0, no `<select>` anywhere on the board, `Remove` still works, drag-and-drop still moves a card between columns and persists. `tsc -b`/`lint`/`build` clean.
- **Follow-up, same session: restored the `Edit` action.** Explicit request to add the `Edit` button back "as it was" — restored `features/task/components/EditTaskForm.tsx` verbatim from before its removal, and re-wired `editingTask` state + the `Edit` button + `Modal`/`EditTaskForm` back into `TaskBoard`. The status `<select>` removal from the previous entry was **not** reverted — status still only changes by dragging a card, or now also via the Status field inside the `Edit` modal (same as `EditTaskForm` always supported). `Remove` (creator-or-owner) is unaffected. Amended into the prior commit rather than a new one, per explicit instruction.
  - Verified: `tsc -b`/`lint`/`build` clean.

## Current Task
- V1 Auth UI, Workspace/Project UI, the "Ledger Desk" redesign, and V4 Task Management (Kanban board, priority, drag-and-Edit-modal status changes) are all committed and verified end-to-end against the live backend.

## Next Steps
- Still open, all previously deferred: edit/rename UI for workspaces/projects, and member invitation UI (backend supports it; `useWorkspaceMembers` now exists frontend-side too, from building the assignee dropdown, so an invite form would have less new plumbing to add than before).

## Important Notes
- Keep all future work scoped to the current milestone unless explicitly requested.
- Update this file after each completed task.
- The backend (`../backend`) already has CORS enabled for this frontend's dev origin (`http://localhost:5173` by default).
- **Test-data cleanup must be scoped to an exact, single-run tag, never a blanket sweep** — a cleanup script during this session's design work queried "any remaining workspace in the dev DB" and deleted one that turned out to belong to the real developer account, not a test artifact. Unrecoverable (no soft-delete/history table). See `backend/.claude/progress.md`'s Important Notes for the full account.
